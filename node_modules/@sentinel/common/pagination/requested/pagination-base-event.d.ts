import { IPaginationEvent, SortDir } from './i-pagination-event';
export declare abstract class PaginationBaseEvent implements IPaginationEvent {
    size: number;
    sort?: string;
    sortDir?: SortDir;
    protected constructor(size: number, sort?: string, sortDir?: SortDir);
}

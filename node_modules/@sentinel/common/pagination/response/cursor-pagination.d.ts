import { PaginationBase } from './pagination-base';
import { ICursorPagination } from './i-cursor-pagination';
export declare class CursorPagination extends PaginationBase implements ICursorPagination {
    curr: string | null;
    next: string | null;
    previous: string | null;
    constructor(curr: string | null, numberOfElements: number, size: number, totalElements: number, next: string | null, previous: string | null);
}

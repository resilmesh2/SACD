import { IOffsetPagination } from './i-offset-pagination';
import { ICursorPagination } from './i-cursor-pagination';
export interface IPagination {
    numberOfElements: number;
    size: number;
    totalElements: number;
}
export declare function isOffsetPagination(pagination: IPagination): pagination is IOffsetPagination;
export declare function isCursorPagination(pagination: IPagination): pagination is ICursorPagination;

import { Observable } from 'rxjs';
import { ApolloResult, EntityUpdateEvent } from '@sentinel/common/graphql';
export interface EntityPatchService<EntityPatch> {
    patch(event: EntityUpdateEvent<EntityPatch>): Observable<ApolloResult>;
}

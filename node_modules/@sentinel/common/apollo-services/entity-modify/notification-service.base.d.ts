import { NotificationType } from '@sentinel/common/utils';
import { Observable } from 'rxjs';
export interface NotificationServiceBase {
    emit(type: NotificationType, title: string, ...args: unknown[]): Observable<boolean>;
}

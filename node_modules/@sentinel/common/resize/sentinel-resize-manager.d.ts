import { ResizableTarget, SentinelResizeObserver } from './sentinel-resize-observer';
export declare class SentinelResizeManager {
    private observerThrottleTime;
    private _buffer;
    constructor(observerThrottleTime?: number);
    observe(target: ResizableTarget, throttleTime?: number): SentinelResizeObserver;
    get root(): SentinelResizeObserver;
}

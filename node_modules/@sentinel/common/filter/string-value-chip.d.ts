import { FilterChip } from './filter-chip';
export declare class StringValueChip extends FilterChip {
    /**
     * Optional attribute to differentiate string value chips of different meaning
     */
    id?: string;
    constructor(value: string, id?: string);
}

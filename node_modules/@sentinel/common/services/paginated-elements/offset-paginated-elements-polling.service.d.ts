import { PaginatedElementsPollingService } from './paginated-elements-polling.service';
import { IPaginatedElements, OffsetPagination } from '@sentinel/common/pagination';
export declare abstract class OffsetPaginatedElementsPollingService<T> extends PaginatedElementsPollingService<T> {
    protected lastPagination?: OffsetPagination;
    protected initSubject(pageSize: number): IPaginatedElements<T>;
}

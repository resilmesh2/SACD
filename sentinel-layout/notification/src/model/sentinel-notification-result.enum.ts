/**
 * Represents result of the notification. Dismissed if user did not interact with it, confirmed if user selected optional action
 */
export enum SentinelNotificationResult {
  DISMISSED = 'dismissed',
  CONFIRMED = 'confirmed',
}

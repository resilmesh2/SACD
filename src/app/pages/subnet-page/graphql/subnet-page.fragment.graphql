fragment SubnetPageChildIP on IP {
  address
  version
  subnets {
    range
  }
  nodes {
    host {
      software_versions {
        version
        vulnerabilities {
          cve {
            cve_id
          }
        }
      }
    }
  }
}
